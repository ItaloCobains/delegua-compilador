var soma = funcao(a, b) {
    retorna a + b;
};

var multiplicar = funcao(a, b) {
    retorna a * b;
};

escreva("Soma direta: " + texto(soma(10, 5)));
escreva("Multiplicação direta: " + texto(multiplicar(10, 5)));
escreva("Soma novamente: " + texto(soma(20, 3)));
